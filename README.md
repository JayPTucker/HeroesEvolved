# Commands

.\gradlew.bat runClient

.\gradew.bat build

Time Stopping:
uses /tick freeze
* Level 1 can only keep time stopped for 5-10 seconds
* Level 1 can teleport a max of ___ distance or it just consumes A LOT of stamina